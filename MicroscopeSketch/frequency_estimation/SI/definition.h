#ifndef DEFINITION_H
#define DEFINITION_H

#define TUPLE_LEN 4
#define TRACE_LEN 16
#define PACKET_NUM 0x7fffffff
#define min(x, y) (x < y? x:y)
#define max(x, y) (x > y? x:y)

#endif // DEFINITION_H
